<% unless task.archived? %>
  <p>
    <strong>Task:</strong>
    <%= task.body %>
  </p>

  <p>
  <%= link_to "Destroy Task", [task.article, task], data: {
                turbo_method: :delete,
                turbo_confirm: "Are you sure?"
              } %>
</p>
<% end %>